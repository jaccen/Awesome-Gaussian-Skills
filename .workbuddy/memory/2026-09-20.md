
## 2026-09-20 20:05 - 项目 skills 同步到本机智能体
- 将 skills/ 下 15 个 skill 用 robocopy /MIR 镜像同步到 ~/.workbuddy/skills/（含 _contracts）。
- 顺带清掉了旧安装遗留的嵌套重复目录（如 3dgs-method-compare/3dgs-method-compare/）。
- 逐项核对源/目标文件数一致；.disable_to_model_invocation_migration.json 未动。
- 坑：本会话 bash coreutils 缺失（ls/dirname 不可用），PowerShell 工具 stdout 为空，核对结果需写临时文件再 Read。
