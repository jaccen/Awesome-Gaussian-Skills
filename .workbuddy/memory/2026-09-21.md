
# 2026-09-21 工作日志

## v0.9.4 两波改动：多渠道抓取 + 裁定波

**第一波（700 → 862）**
- 多渠道 arXiv 抓取：`scripts/harvest_new_papers.py`（6 个检索式，395 篇候选），
  `scripts/verify_candidates.py` R1–R6 核验，`scripts/enroll_new_papers.py` 入库 158 条。
- 关键 bug：arXiv `submittedDate` 必须用 `YYYYMMDDHHMM`（带横杠直接 400，表现为「全体 HTTPError / 候选池 0 篇」）。
  `scripts/daily_arxiv_scan.py` 同一 bug 已修。
- references 修正 57 条「显示 ID ≠ 链接 ID」；删除 13 条错挂（如 `Dynamic 3D Gaussians` 指向磁性自旋玻璃物理论文）。
- AIGC 水印全清：29 文本文件 + 21 张 JPEG。
- `README_CN.md` 双重编码乱码不可本地还原 → 用上游 GitHub raw 完好版替换（本地还原留 1382 个 U+FFFD）。

**第二波裁定（862 → 859）**
- `GeoGS-SLAM` 是**两篇无关联论文同名**：2607.07452（Zhou 等，Geometry-Only）与
  2607.11184（Gao 等，Geometric Priors），作者零重叠，非续作。
  → 07452 用本名，11184 改 `GeoGS-SLAM (Geometric Priors)`（消歧词取自各自副标题）。
- 派生后缀扫描 11 条：7 条为作者自述后缀（FreeTimeGS++/MesonGS++/P2M++/ReconDreamer++/
  SpatialAvatar-0/StereoSplat+）保留；`Scaffold-GS+`、`GaussianSplatting-SLAM-v2` 查无此文 → 删除；
  `Luminance-GS++`(2602.18322) 是同作者组 TPAMI 扩展版 → 正名 `Luminance-GS`。
- `Spark 2.0` 原描述误写为「NVIDIA 机器人仿真」→ 实为 World Labs 开源 WebGL2 3DGS 渲染引擎
  （sparkjs.dev / sparkjsdev/spark），desc+venue+category+code_url 全部修正。
- `HeadsUp`(2605.04035, ECCV 2026)：json 中已以描述性名 `Large-Scale HQ 3D Gaussian Head` 存在
  （sources 含 `alias:HeadsUp`），补录会造成重复 → 改为把原条目正名。
  **教训：补录前必须按 arxiv_id 查重，不能只按 name 查重。**
- references 973 → 859：去重 106 条（旧去重只认带 arXiv 链接的行），
  再删 3 条「论文真实但无自述方法名」的条目（Gaussian-Enhanced Surfel / View-Dependent
  Splatting Kernels / Ambient-Robust Inverse Rendering）+ `RAF`（无 ID，不可核实）。

## 新增工具（保留）
`adjudicate_duplicates.py`（裁定落库）、`reconcile_references.py`（references 对齐）、
`prune_abstracts.py`（abstracts.js 键校对）、`crosscheck_carriers.py`（**名称集合**比对，非仅计数）、
`fix_headsup.py`（重复合并）、`final_fix.py`（收尾：references 去重+陈旧计数）。
注：`_audit_derived.py` / `_verify_derived.py` 等 `_` 前缀探针为一次性探索脚本，已在收尾清理中删除。

## 收尾清理（最终可提交）
- 删除 104 个 `*.bak*` 备份、`scripts/_*.py` 一次性探针（14 个）、`.temp/aigc-backup/`（AIGC 清理前备份）、空 `_verify/`。
- 保留 `scripts/` 下结构化工具（sync/harvest/verify/enroll/adjudicate/reconcile/prune/final_fix/crosscheck/final_check）。
- 清理后三套校验器仍全绿：methods.json 859 / CSV 859 / methods.html 859 / abstracts.js 859 / references 859；
  validate_knowledge_base PASS（arXiv 749/87%，代码 133/15%）；AIGC 残留 0；版本统一 v0.9.4。

## README 乱码修复（用户指出 readme 存在乱码）
- **根因**：`README.md`（英文版）整篇中文与符号都是**双重编码损坏**（UTF-8 字节被按 GB18030 误读）。
  此前 replaced README_CN.md 用上游完好版，但 README.md 一直未被修。
- **损坏范围**：技能示例表 15 行中文示例、74 处 em-dash `—`（鈥?）、54+ 处 arrow `→`（鈫?）、
  superscript `²`（虏）、`中文` 链接标签（涓枃）、`↔` 等。
- **修复**：gb18030→utf-8 逆向还原 + 语境重建（不可逆处），按行号定向替换，并补回符号后丢失的空格。
  `scripts/scan_aigc_mojibake.py` 复扫确认 README.md 残留乱码 = 0；README_CN.md 仅第 106 行是
  叙述性举例（`鏂规硶` 描述损坏样貌），非实际乱码。
- **AIGC 复审**：交付文件无真正 AIGC 水印（frontmatter `AIGC:` 键仅在 `.temp/登记附件3_...md`
  工作文件里，非交付物）。其余 scan 命中的 `AIGC`/`AI生成` 全是：AIGC 清理工具脚本的搜索串、changelog
  叙述清理过程、以及合法正文/代码注释（讨论水印溯源、AIGC 检测、AI 生成音乐特性等）。

## 环境坑（重要）
- **bash heredoc 会吃掉反斜杠**：`cat > x.py <<'EOF'` 写入的正则里 `\\s` 变成 `/s`、`\\` 变成 `//`，
  脚本静默失效（表现为「methods.html 名称匹配 0 条」）。含正则/转义的脚本一律用 Write 工具写。
- 命令行内联 `python -c "...\""` 里的引号被 shell 破坏 → 同样改用脚本文件。
- `unesc = s.encode().decode("unicode_escape")` 会把 `³`/`é` 二次编码成 `Â³`/`Ã©`，
  解析 JS/HTML 字面量时只能用 `re.sub(r"\\(.)", r"\1", s)`。

## 核验协议新增
- R7 派生后缀必须对照 arXiv **标题**确认，作者没印出来的后缀＝伪名。
- R8 同名先看**作者重叠**；作者不重叠＝两种方法，消歧词取自各自副标题而非版本号。
- R9 论文未自述方法名的（描述性标题）不入目录，宁删勿编。
